package com.lnmcp.lena.config;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * This class was previously used for Redis testing configuration.
 * It has been disabled since we're now using a simple in-memory cache.
 */
@TestConfiguration
public class TestRedisConfiguration {
    // Configuration disabled - using in-memory cache instead
}
